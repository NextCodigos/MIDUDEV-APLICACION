/* Estilos para el contenedor de navegación */
nav {
  display: flex; /* Establece el contenedor como un flexbox */
  flex-direction: column; /* Define la dirección de los elementos como columna */
  align-items: center; /* Centra los elementos horizontalmente */
  justify-content: space-between; /* Distribuye el espacio entre los elementos verticalmente */
  width: 100%; /* Establece el ancho del contenedor al 100% del contenedor padre */
  text-align: center; /* Alinea el texto al centro dentro del contenedor */
}

/* Estilos para los enlaces dentro del nav */
nav a {
  color: white; /* Color del texto */
}